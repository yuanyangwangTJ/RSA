#include "RSA.h"
#include "AES.h"
#include <NTL/ZZ.h>

using namespace NTL;
using namespace std;

int main()
{

    AES T;
    // RSAUser Alice, Bob;
    // Alice.GenerateKey();
    // Alice.SendPublicKey(Bob);
    // Bob.EncryptMessage();
    // Bob.SendMessage(Alice);
    // Alice.DecryptMessage();

    return 0;
}